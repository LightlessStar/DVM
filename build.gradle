plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'junit:junit:4.13.2'
}

test {
    useJUnitPlatform()  // JUnit 5용
    reports {
        junitXml.required.set(true)
        html.required.set(true)
    }
}
